import {
  getDriver,
  getDrivers,
  createDriver,
  updateDriver,
  deleteDriver,
} from "./driver";

import {
  getVehicle,
  getVehicles,
  createVehicle,
  updateVehicle,
  deleteVehicle,
} from "./vehicle";

export const jsonServer = {
  getDriver,
  getDrivers,
  createDriver,
  updateDriver,
  deleteDriver,
  getVehicle,
  getVehicles,
  createVehicle,
  updateVehicle,
  deleteVehicle,
};
